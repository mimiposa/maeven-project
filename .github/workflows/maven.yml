name: Java CI with Maven

on:
  push:
    branches:
      - "main"
      - "develop"
      - "feature/*"
      - "release/*"
      - "hotfix/*"
  pull_request:
    branches:
      - "main"
      - "develop"

jobs:
  build:
    name: Build and Test my-app Application
    
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      
      # Set up JDK 21 with Maven
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      # Cache the Maven dependencies (for more efficiency and speed)
      - name: Cache Maven dependencies
        uses: actions/cache@v3
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      # Build with Maven
      - name: Build with Maven
        run: mvn -B -f my-app/pom.xml clean install

      # Run the tests
      - name: Run tests with Maven
        run: mvn -B -f my-app/pom.xml test

  # Optional: Add notifications for build/test failures
  notify:
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: Send failure notification
        run: |
          echo "Build failed!"
